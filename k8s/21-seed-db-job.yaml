apiVersion: batch/v1
kind: Job
metadata:
  name: seed-db-metrics
  namespace: haic-benchmark
  labels:
    app: seed-db
spec:
  backoffLimit: 3
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: seed-metrics
          image: postgres:15
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              set -e
              echo "==> Waiting for database to be ready..."
              for i in $(seq 1 30); do
                if psql -h postgres -p 5432 -U $DB_USER -d $DB_NAME -c "SELECT 1;" >/dev/null 2>&1; then
                  echo "Database is ready!"
                  break
                fi
                echo "Database not ready (attempt $i/30), waiting..."
                sleep 2
              done

              echo "==> Seeding metrics and definitions..."
              psql -h postgres -U $DB_USER -d $DB_NAME -v ON_ERROR_STOP=1 -f /seed/seed-metrics.sql

              echo "==> Seed data applied successfully!"
          env:
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: haic-benchmark-secret
                  key: DB_PASSWORD
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: haic-benchmark-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: haic-benchmark-secret
                  key: DB_PASSWORD
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: haic-benchmark-secret
                  key: DB_NAME
          volumeMounts:
            - name: seed
              mountPath: /seed
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      volumes:
        - name: seed
          configMap:
            name: db-seed-metrics
