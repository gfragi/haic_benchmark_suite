# DEPRECATED — use frontend_ingress.yaml instead (wrong issuer + old hostname)
# frontend-ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-ingress
  namespace: benchmarking
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: kubeflow-self-signing-issuer 
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - hua-benchmarking.ddns.net
      secretName: frontend-tls
  rules:
    - host: hua-benchmarking.ddns.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
