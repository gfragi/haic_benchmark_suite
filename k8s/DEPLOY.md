# HAIC Benchmark Suite — Kubernetes Deployment

This file used to duplicate the deployment runbook and had drifted out of sync
with the actual manifests (wrong namespace, references to files that no
longer exist, instructions for the retired Vue frontend). To avoid that
happening again, there is now a single source of truth:

**See [`README.md`](README.md) in this directory.**
