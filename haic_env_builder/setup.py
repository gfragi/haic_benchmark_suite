from setuptools import setup, find_packages

setup(
    name="haic_env_builder",
    version="0.1",
    description="Environment builder for HAIC simulation environments",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)
