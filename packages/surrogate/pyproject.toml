[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "surrogate"
version = "0.1.0"
description = "Probabilistic surrogate agents fitted from real pilot data"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "scipy>=1.10"]
