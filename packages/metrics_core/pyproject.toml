[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "metrics-core"
version = "0.1.0"
description = "Shared HAIC metrics (Core v1)"
requires-python = ">=3.10"
dependencies = ["pydantic>=2,<3"]